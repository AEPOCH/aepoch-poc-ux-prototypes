// src/prototypes/example.tsx
// Replace this file with a real prototype from Claude.
// The default export is what gets rendered.

export default function ExamplePrototype() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="text-center p-8">
        <div className="text-5xl mb-4">👋</div>
        <h2 className="text-2xl font-semibold mb-2">Pipeline is working</h2>
        <p className="text-gray-500 max-w-sm">
          Replace this file with a real prototype exported from Claude.
          Make sure it has a default export.
        </p>
      </div>
    </div>
  )
}
